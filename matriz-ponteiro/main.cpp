#include <iostream>
#include <fstream>

using namespace std;


int main (){
	int colunas, **matriz,
	matriz = new int*;

	ofstream arq;
	arq.open("vetor.txt", ios:: out );
		if (!arq.is_open()) return 0;
	getline (arq, i);
	int linha[i]
	while (arq.good())
		
	 




return 0;
}
